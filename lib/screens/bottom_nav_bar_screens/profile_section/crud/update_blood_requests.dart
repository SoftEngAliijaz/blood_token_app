import 'package:flutter/material.dart';

class UpdateBloodRequestScreen extends StatelessWidget {
  const UpdateBloodRequestScreen({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('UpdateBloodRequestScreen'),
      ),
      body: Center(
        child: Text('UpdateBloodRequestScreen'),
      ),
    );
  }
}
